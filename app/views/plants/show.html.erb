<div class="container-fluid bg-white">
  <div class="container">

  <!-- the next is a ROW DIV for the PLANT IMAGE as Banner -->
    <div class="row mt-4">
      <div class="col">
        <!-- here I want an image -->
        <div class="banner-plant-show rounded-lg" style="background-image: url('<%= cl_image_path @plant.photo.key %>')">
          <!-- <%#= cl_image_tag @plant.photo.key, :transformation=>{:width=>1200, :height=>500, :crop=>"fill"} %> -->
          <%#= cl_image_tag @plant.photo.key %>
          <!-- here I want a black H1 title with a square white background -->
          <h1 class="text-center bg-white rounded-lg"><%= @plant.name %></h1>
        </div>

        <div class="mt-4">

          <p class="text-center">Water: <%= @plant.water %></p>
          <p class="text-center">Light: <%= @plant.light %></p>
          <p class="text-center">Soil: <%= @plant.soil %></p>
          <p class="text-center">Fertilizer: <%= @plant.fertilizer %></p>
          <p class="text-center">Repotting: <%= @plant.repotting %></p>
        </div>

      </div>
    </div>
    <!-- the next is a ROW DIV for the PLANT ATTRIBUTE as Section-->
    <div class="row mt-4">

    </div>
    <!-- the next is a ROW DIV for the BUTTON to ADD the plant to the User Jungle Index or to create a Jungle -->
    <div class="row mt-4">

    </div>
  </div>
</div>
