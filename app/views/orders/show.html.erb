<div class="container-payment">
  <div class="row py-4"><!--
    <div class="col-md-3"><%#= image_tag @order.teddy.photo_url, width: '100%' %></div> -->
    <div class="col">
      <h1>Order #<%= @order.id %> <small><span class="badge badge-primary"><%#= @order.state %></span></small></h1>
        <ul class="list-unstyled">
          <li><strong>ISSUED ON</strong> <%= @order.created_at.to_date %></li>
          <!-- <li><strong>Booking</strong> --> <%#= @order.teddy. %></li>
          <li><strong>TOTAL</strong> <%= humanized_money_with_symbol @order.amount %></li>
          <li><strong>KEYS</strong> Contact our Plantkeepers now via our chat! Otherwise we will contact you in the next <strong>24h</strong>.</li>
      </ul>
     <%= link_to "My Bookings", dashboards_index_path, class: "btn btn-lg yellow-button shadow px-5 py-3 mb-5" %>
    </div>
  </div>
</div>
